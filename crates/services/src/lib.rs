//! `services` — signaling / session-registry / matchmaking WebSocket service.
//!
//! Stub for Phase 1 scaffolding. No functional code yet. (Becomes a binary in Phase 5.)

#[cfg(test)]
mod tests {
    #[test]
    fn smoke() {
        assert_eq!(2 + 2, 4);
    }
}
