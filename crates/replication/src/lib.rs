//! `replication` — the custom protocol: wire format, quantization, delta/baseline,
//! authority, handoff, resync. **HIGH-RISK.**
//!
//! Stub for Phase 1.1. No functional code yet. See crates/replication/CLAUDE.md
//! before touching: "compiles but subtly wrong" is the dominant risk here.

#[cfg(test)]
mod tests {
    #[test]
    fn smoke() {
        assert_eq!(2 + 2, 4);
    }
}
